"use client";import {useState} from "react";import {useRouter} from "next/navigation";import {CATEGORIES} from "../lib/site";
export default function AdminForm(){const [message,setMessage]=useState(""),[busy,setBusy]=useState(false),router=useRouter();async function submit(e){e.preventDefault();setBusy(true);setMessage("");const r=await fetch("/api/posts",{method:"POST",body:new FormData(e.currentTarget)});const d=await r.json();setMessage(d.message||"Fehler");if(r.ok){e.currentTarget.reset();router.refresh()}setBusy(false)}return <form className="form" onSubmit={submit}><h2>Neuen Bericht veröffentlichen</h2>{message&&<div className="notice">{message}</div>}<label>Titel</label><input name="title" required/><label>Kategorie</label><select name="category">{CATEGORIES.map(c=><option key={c}>{c}</option>)}</select><label>Bericht</label><textarea name="content" required/><label>Titelbild (optional)</label><input name="image" type="file" accept="image/*"/><br/><br/><button className="btn" disabled={busy}>{busy?"Wird gespeichert…":"Bericht veröffentlichen"}</button></form>}
